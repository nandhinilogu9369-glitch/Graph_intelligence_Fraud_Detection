def generate_users(n=50):
    return [f"user_{i}" for i in range(n)]


def generate_devices(n=25):
    return [f"device_{i}" for i in range(n)]


def generate_ips(n=20):
    return [f"ip_{i}" for i in range(n)]
